name: beecount_accessibility_rules_tools
description: Local tools for importing and sanitizing GKD accessibility snapshots.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  archive: ^3.6.1

dev_dependencies:
  test: ^1.25.0
